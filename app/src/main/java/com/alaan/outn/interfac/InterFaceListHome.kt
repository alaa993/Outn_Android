package com.alaan.outn.interfac

import com.alaan.outn.model.ModelListHome

interface InterFaceListHome {

    fun edit(edit: ModelListHome)
    fun delet(delet: ModelListHome)
    fun share(comment: ModelListHome)
}